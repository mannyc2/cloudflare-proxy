{
  "name": "cf-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy to bypass IP-based rate limits",
  "license": "MIT",
  "keywords": [
    "cloudflare",
    "workers",
    "proxy",
    "rate-limit",
    "wayback-machine"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx test/e2e.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.91.0"
  }
}
